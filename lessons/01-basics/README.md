# Lesson 01: 文法とデータ構造

## ゴール

- 変数・関数・配列・オブジェクトの基本を一通り
- `class` の最小例（Pythonと比較しながら）
- `===` / `==` の違い、`undefined` / `null`

## 実行

```bash
npm run lesson:01
```

## 見どころ

- `const` / `let` を基本にする（`var` は原則使わない）
- オブジェクトは「辞書 + メソッド」感覚でまずOK
- `class` は糖衣構文（プロトタイプベースの上にある）
